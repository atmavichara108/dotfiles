---
description: Пайплайн создания плагинов — для nvim, rofi, btop и других инструментов. builder → reviewer.
agent: builder
subtask: true
---

# Пайплайн: Создание плагина

Задача: $ARGUMENTS

## Шаг 1: Анализ

Прочитай `AGENTS.md`, `.opencode/memory/user-profile.md`, документацию целевого инструмента.
Оцени: какой тип плагина нужен, куда положить.

## Шаг 2: Реализация

Реализуй плагин:
- Определи целевую директорию (`nvim/`, `rofi/`, `btop/`, etc.)
- Следуй конвенциям инструмента
- Добавь README если нужно

## Шаг 3: Проверка

Вызови reviewer:
```
task(agent="reviewer", prompt="review: плагин — $ARGUMENTS. Проверь стиль, безопасность, stow-совместимость.")
```

## Шаг 4: Финализация

Если PASS → `git add`, diff, коммит.
Если FAIL → повтор цикла (макс 3).
